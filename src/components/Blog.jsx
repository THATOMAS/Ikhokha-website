import React from "react"
import styled from "styled-components"


const BlogContainer = styled.article`
height:100vh;
background:blue;`

const Blog = ()=>{
	return(
		<BlogContainer />
		)
}

export default Blog;